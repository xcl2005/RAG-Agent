# 星河工坊：发布流程（虚构教学资料）

星河工坊是本评测专用的虚构工程团队；以下规则不是任何真实企业制度。

## 发布与回滚

服务部署先进入预发布环境。发布流水线必须通过单元测试、类型检查和检索回归，再由值班工程师确认上线。灰度发布先分配 5% 流量，观察 10 分钟。

如果灰度期间错误率超过 2%，停止放量并回滚到上一稳定镜像。回滚只切换应用镜像，不自动撤销数据库迁移。数据库变更需要先保证新旧代码都可读取。

## 版本记录

每次发布记录 Git 提交号、镜像摘要和评测报告。验收说明写明已知失败案例，不能只附成功问答截图。
