# 星河工坊：一次重复计费演练（虚构教学资料）

## 现象和根因

演练时客户端收到 503 后重新生成 request_id 再提交，导致同一逻辑请求两次计费。根因是客户端重试破坏了幂等约定，不是模型回答了两遍同样的话。

## 修复和验证

客户端改为保留原 request_id。测试注入一次 503，然后检查重试复用标识且最终只有一条计费记录。服务端把租户标识加入幂等缓存键，防止跨租户复用结果。

## 复盘限制

这是本地故障演练，只有构造的请求记录，没有线上用户或真实营收数据。报告不能写成真实生产事故，也没有证明可以承受万人并发。
