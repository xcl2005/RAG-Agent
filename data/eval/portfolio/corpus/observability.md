# 星河工坊：运行观测（虚构教学资料）

## 追踪与指标

每个查询生成 trace_id，记录检索、模型调用、引用校验各阶段耗时。错误日志记录错误类别和追踪标识，避免写入凭证。运行看板分别统计 p50 和 p95 延迟，p95 表示 95% 请求的耗时不超过该值。

## 模型预算

记录输入 token、输出 token 和模型调用次数。若提供商返回推理 token，单独记账，不把它当作用户可见答案。模型输出为空或被长度限制截断时标为生成失败。

## 告警

连续 5 分钟生成失败率超过 3% 时告警。告警先检查模型完成原因和剩余预算，再查看检索质量；不能把所有失败统一统计为拒答。
