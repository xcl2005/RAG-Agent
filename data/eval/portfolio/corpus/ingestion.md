# 星河工坊：资料入库（虚构教学资料）

## 分块和索引

资料入库采用按段落递归分块，默认每块 600 字符，相邻块重叠 80 字符。重叠用于保留上下文，也会增加索引体积。扫描 PDF 若无法提取正文，标为需要 OCR，不宣称入库完成。

## 内容身份与替换

文档内容使用 SHA256 校验。相同来源且内容未变化时跳过重复入库；内容变化时替换该文档的旧分块。SQLite 保存正文和版本清单，向量库只保存向量及轻量元数据。

## 删除

删除先从 SQLite 移除正文，再清理对应向量。向量清理失败会留下待重试状态；检索结果必须通过正文库校验，避免已删除资料继续成为证据。
