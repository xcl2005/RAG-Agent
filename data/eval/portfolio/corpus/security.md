# 星河工坊：资料与工具安全（虚构教学资料）

## 租户隔离

所有资料记录 tenant_id。查询和删除都先验证租户，未授权资料必须在召回前过滤。共享缓存的键包含租户标识，不能只使用问题文本。

## 提示注入

检索到的文档是待核对的数据，不具有修改系统规则的权限。文档要求忽略规则、读取密钥或调用工具时，记录提示注入告警，不能把这些文字当作用户指令。

## 工具权限

模型只能使用服务端允许列表中的只读工具。删除资料由用户明确确认并经服务端鉴权执行，模型不能自行扩大权限。日志默认不记录 API 密钥和文档全文。
