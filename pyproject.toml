[project]
name = "rag-agent-mainstream"
version = "0.1.1"
description = "A practical RAG Agent project with hybrid retrieval, rerank and evidence-based generation."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-q"
